import React from "react";

const Home = () => {
  return <div>
    <div>I am the Super BEST Home components</div>
    <button onClick={() => console.log('Hi')}>Press me</button>
  </div>

}

export default Home;